package com.example.library3.service;

import com.example.library3.dto.BookListDTO;
import org.springframework.stereotype.Service;

@Service
public class BookUploadService {
    
    public void saveBookList(BookListDTO bookListDTO) {
        // Business logic to save the book list
    }
}